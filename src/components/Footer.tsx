export function Footer() {
  return (
    <footer className="site-footer">
      <p>Built on Stellar Testnet</p>
      <p>
        Testnet only — assets have no real-world value. Never share a secret key
        or recovery phrase.
      </p>
    </footer>
  )
}
